import styled, { css } from "styled-components";

const container = css`
  width: 100%;
  max-width: 320px;
  display: flex;
  flex-direction: column;
`;

export const HomeContainer = styled.div`
  ${container}
`;
